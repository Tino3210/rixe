<Window x:Class="Rixe.MainMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rixe"
        mc:Ignorable="d"
        Title="Rixe" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Grid.Column="0" Grid.Row="0" Text="{Binding Capture}"></TextBlock>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="100" FontWeight="Bold" Grid.Column="1" Grid.Row="0">Rixe</TextBlock>
        <Button Name="server" Margin="30,20" Grid.Column="1" Grid.Row="1">Play as Host</Button>
        <Button Name="client" Margin="30,20" Grid.Column="1" Grid.Row="2">Play as Guest</Button>
        <Button Name="quit" Margin="30,20" Grid.Column="1" Grid.Row="3" Command="{Binding Test}" CommandParameter="{Binding}">Quit</Button>
    </Grid>
</Window>
